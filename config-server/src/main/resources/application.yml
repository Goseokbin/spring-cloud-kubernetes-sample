
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Goseokbin/spring-cloud-kubernetes-sample
          searchPaths: config-repo
server:
  port: 8671

logging:
  level:
    root: DEBUG